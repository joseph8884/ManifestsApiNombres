apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-front-service
spec:
  selector:
    app: front
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.front.port }}
  type: ClusterIP