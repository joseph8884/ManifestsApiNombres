apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-helm-chart2.fullname" . }}-front-service
spec:
  selector:
    app: frontprod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5173  # Update with your app's port
  type: ClusterIP